{
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "${VITE_API_URL}/api/:path*"
    },
    {
      "source": "/auth/:path*",
      "destination": "${VITE_API_URL}/auth/:path*"
    }
  ]
}